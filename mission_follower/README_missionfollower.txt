mission follower readme


graph_nav_client.download_graph() asks robot for every waypoint in memory

graph.waypoints is a python list containing all the waypoints. Should be sorted by time
    graph.waypoint[0] first waypoint
    graph.waypoint[-1] last waypoint
    