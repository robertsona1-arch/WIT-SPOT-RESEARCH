README for varying_height_map


base_offset_rt_body moves the body relative to itself, invalid for stand command

base_offset_rt_footprint is what we want


need to make it so the amount of snapshots stay consistent per map, maybe 6 snapshots, and change the range that it takes the pictures. 